# Git常用命令总结
## git init
    执行git初始化，在当前文件夹创建.git文件夹。
## git clone
    将git远程仓库克隆到本地。
## git status
    查询仓库状态。
## git add
    将改动添加到暂存区（staging area），commit时提交的改动是上一次加入到暂存区的改动，而不是硬盘中的改动。
    会递归添加当前工作目录中的所有文件。
## git commit
    提交已经被add进来的改动。
    